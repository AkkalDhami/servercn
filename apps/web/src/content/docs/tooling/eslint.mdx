---
title: ESLint
description: A robust ESLint configuration for TypeScript and Express.js projects.
command: npx servercn add eslint
---

# ESLint

ESLint is a static code analysis tool for identifying problematic patterns found in JavaScript/TypeScript code. It helps maintain code quality and consistency across your project.

## Installation

Install the ESLint configuration using the ServerCN CLI:

<PackageManagerTabs command="npx servercn add eslint" />

## Configuration

The component provides a pre-configured `.eslintrc.json` (or `eslint.config.js`) tailored for Node.js and TypeScript development.

### Example Configuration

```json
{
  "root": true,
  "parser": "@typescript-eslint/parser",
  "plugins": ["@typescript-eslint"],
  "extends": [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended",
    "prettier"
  ],
  "rules": {
    "no-console": "warn",
    "@typescript-eslint/no-explicit-any": "error",
    "@typescript-eslint/explicit-function-return-type": "off"
  }
}
```

## Scripts

Add the following scripts to your `package.json`:

```json
{
  "scripts": {
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix"
  }
}
```

## Features

- **TypeScript Support**: Full support for TypeScript syntax and rules.
- **Recommended Rules**: Includes industry-standard recommended rules.
- **Prettier Integration**: Configured to work seamlessly with Prettier.
- **Error Prevention**: Catches common errors before they reach production.
